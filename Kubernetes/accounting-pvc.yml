apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: accounting-sql-db-pvc
  namespace: itonk_tecais
spec:
  resources:
    requests:
      storage: 3Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: tecais-storage-class
